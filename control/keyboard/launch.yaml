import:
  - control/action/launch.yaml

control:
  keyboard_action:
    ${mod}-Return: launch_terminal
    ${mod}-Shift-Return: launch_sudo_terminal
    
    ${mod}-i: launch_browser
    ${mod}-e: launch_filer
    
    ${mod}-a: action_menu
    ${mod}-p: launch_menu
